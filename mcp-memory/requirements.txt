# Base dependencies for pure JSON-RPC server
# Memory system dependencies (optional - will fallback if not available)

# PostgreSQL adapter for memory system
psycopg[binary]>=3.1.0

# FastAPI for web server
fastapi>=0.109.2
uvicorn>=0.27.1
python-multipart>=0.0.9
pydantic>=2.6.1

# Note: 
# - MCP server works without memory system (pure JSON-RPC)
# - Memory system requires PostgreSQL + pgvector
# - Embedding requires Ollama (optional - falls back to keyword-only)